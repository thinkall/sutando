{
  "name": "report-feedback",
  "version": "1.2.0",
  "owner": "github:sonichi/sutando",
  "stability": "stable",
  "description": "Bug reports to Agent Universe under the owner's cloud identity, with the owner's Settings toggles as the consent surface. Ask-first parks a draft and registers its card in the HITL store; the Stop hook applies the owner's click at the end of the agent's turn. Automatic reports wait for existing recovery outcomes or one hour without recovery; deploy the periodic deadline worker for idle cores.",
  "permissions": {
    "network": true,
    "filesystem": "read-write",
    "secrets": [
      "AG2_CLOUD_TOKEN"
    ]
  },
  "hooks": [
    {
      "event": "Stop",
      "command": "./hooks/apply-clicks.py"
    }
  ]
}
